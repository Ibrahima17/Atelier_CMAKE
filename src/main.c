#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "./../include/calculator.h"


int main() {
    int result = carre(5); // Appel de la fonction au carré avec 5
    printf("Le carré de 5 est : %d\n", result);
    return 0;
}
